package com.example.android.effectivenavigation;

import android.os.Bundle;
import android.support.v4.app.Fragment;
import android.view.LayoutInflater;
import android.view.View;
import android.view.ViewGroup;

public class Dashboard extends Fragment {
	
	public void nextShutdown() {
		SendJson(CommandTypes.ShutdownSchedule, "");
	}



	public void SendJson(CommandTypes CommandType, String Parameter) {
		RabbitConsumeHelper tcpHelper = new RabbitConsumeHelper();
		TransferData transferData = new TransferData();
		transferData.CommandType = CommandType.index();
		transferData.Parameter = Parameter;
		tcpHelper.execute(transferData.ToJson(), getActivity());
	}

	@Override
	public View onCreateView(LayoutInflater inflater, ViewGroup container,
			Bundle savedInstanceState) {
		View rootView = inflater.inflate(
				R.layout.dashboard, container, false);
		
		rootView.findViewById(R.id.refreshDashboard).setOnClickListener(
				new View.OnClickListener() {
					@Override
					public void onClick(View view) {
						switch (view.getId()) {
						case R.id.refreshDashboard:
							nextShutdown();
							break;
						}
					}
				});
		
		

		return rootView;
	}
}